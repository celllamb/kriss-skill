# PPTX Test Fixtures

Tests create synthetic PPTX packages programmatically at runtime. Binary fixture files are intentionally not stored here so each test can build exactly the broken package state it needs.
